<?xml version="1.0" encoding="utf-8"?>

<mct:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
           xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
           xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
           xmlns:system="clr-namespace:System;assembly=System.Runtime"
           xmlns:vm="clr-namespace:SchnapsSchuss.ViewModels"
           xmlns:views="clr-namespace:SchnapsSchuss.Views"
           x:Class="SchnapsSchuss.Views.LeavingPopUp"
           x:TypeArguments="system:Boolean" >

    <mct:Popup.Resources>
        <Style TargetType="{x:Type views:LeavingPopUp}">
            <Setter Property="WidthRequest" Value="800" />
            <Setter Property="HeightRequest" Value="600" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="BackgroundColor" Value="{StaticResource White}" />
        </Style>
    </mct:Popup.Resources>

    <Grid RowDefinitions="Auto,*" Padding="0" Margin="0">

        <Label 
            Grid.Row="0"
            Text="{Binding LabelText}"
            HorizontalTextAlignment="Center"
            FontSize="Large" 
            Margin="0,0,0,10"
            TextColor="Black"
            />

        <Grid Grid.Row="2" ColumnDefinitions="*,*" ColumnSpacing="10" >

            <ContentView Grid.Column="0" Grid.Row="1">
                <!-- Left Column: Purchased Articles with border -->
                <Border Grid.Column="0" Stroke="{StaticResource Black}" StrokeThickness="2" Margin="0,0,10,0">
                    <Grid RowDefinitions="Auto,2,*,2,Auto">
                        <!-- Header Row -->
                        <Grid Grid.Row="0" ColumnDefinitions="*,70,50" ColumnSpacing="12" BackgroundColor="{AppThemeBinding Light={StaticResource Tertiary}, Dark={StaticResource TertiaryDark}}" Padding="10" >
                            <Label FontSize="20" Text="Name" Grid.Column="0" FontAttributes="Bold" VerticalTextAlignment="Center" />
                            <Label FontSize="20" Text="Menge" Grid.Column="1" FontAttributes="Bold" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                            <Label FontSize="20" Text="Preis" Grid.Column="2" FontAttributes="Bold" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                        </Grid>
                        <BoxView Grid.Row="1" HeightRequest="2" BackgroundColor="{StaticResource Black}" Grid.ColumnSpan="3" VerticalOptions="End"/>
                        <!-- Invoice Items Collection -->
                        <CollectionView ItemsSource="{Binding InvoiceItems}" Grid.Row="2">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Vertical" ItemSpacing="5" />
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Grid ColumnDefinitions="*,70,50" ColumnSpacing="12" Padding="10">
                                        <Label FontSize="20" Text="{Binding Article.Name}" Grid.Column="0" />
                                        <Label FontSize="20" Text="{Binding Amount}" Grid.Column="1" HorizontalTextAlignment="Center" />
                                        <Label FontSize="20" Text="{Binding TotalPrice, StringFormat='{0}€'}" Grid.Column="2" HorizontalTextAlignment="Center" />
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                        <BoxView Grid.Row="3" HeightRequest="2" BackgroundColor="{StaticResource Black}" Grid.ColumnSpan="3" VerticalOptions="End"/>
                        <!-- Total Row pinned at bottom with top border -->
                        <Grid ColumnDefinitions="*,Auto" Grid.Row="4" Padding="10">
                            <Label FontSize="20" Text="Gesamt:" FontAttributes="Bold" Grid.Column="0" VerticalTextAlignment="Center" />
                            <Label FontSize="20" Text="{Binding InvoiceTotal, StringFormat='{0}€'}" FontAttributes="Bold" Grid.Column="1" VerticalTextAlignment="Center"/>
                        </Grid>
                    </Grid>
                </Border>
            </ContentView>

            <Grid Grid.Column="1" Grid.Row="1">

                <Grid.RowDefinitions>
                    <RowDefinition Height="2*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Text="Bezahlen"
                        Grid.Row="0"
                        BackgroundColor="{StaticResource Primary}"
                        FontSize="Medium"
                        Command="{Binding PayCommand}"
                        Margin="0,0,0,15"
                    />

                <Button Text="Zurück"
                        BackgroundColor="{StaticResource Tertiary}"
                        Grid.Row="2"
                        FontSize="Medium"
                        Command="{Binding CloseCommand}"
                        TextColor="{StaticResource Black}"
                    />

            </Grid>

        </Grid>

    </Grid>


</mct:Popup>